spring.application.name=course-service
# Server Port Configuration
# We'll run this service on port 8081 to avoid conflicts with the student service later.
server.port=8081

# MySQL Database Connection Settings
spring.datasource.url=jdbc:mysql://localhost:3306/course_enrollment_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=TanujThour@1228

# JPA/Hibernate Settings
# 'update' means Hibernate will automatically update the database schema based on your code.
spring.jpa.hibernate.ddl-auto=update
# This makes the SQL queries generated by Hibernate readable in the console.
spring.jpa.show-sql=true